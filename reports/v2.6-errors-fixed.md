# v2.6 ESLint Errors Fixed

Summary of error-level fixes performed in this run:

- scripts/db-check.js
  - Replaced CommonJS `require()` calls with ESM `import`.
  - Rationale: resolved `@typescript-eslint/no-require-imports` ESLint error.

- src/app/(public)/page.tsx
  - Escaped an ASCII double-quote in JSX (`nicht „laut" sein muss`) to `nicht „laut&quot; sein muss`.
  - Rationale: resolved `react/no-unescaped-entities` error.

Notes:
- After these fixes, a full lint run reports 0 errors and 336 warnings (warnings extracted separately).
- Next recommended steps: triage the top warnings (react-hooks and no-explicit-any are high-impact).
