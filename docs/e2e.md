# E2E (Playwright) – Mini Suite

## Run
1) `npm run dev`
2) `npm run test:e2e`

## Notes
- These tests are intentionally minimal: they confirm key routes are reachable and guards reject unauthenticated access.
- Add authenticated admin tests later (cookie/session injection) once stable.
