"use client";

import * as React from "react";

// Minimal placeholder Toaster mount. If `sonner` is installed, we could
// dynamically import and render it here. For now this avoids build errors
// when `sonner` is not installed.
export default function ToasterMount() {
  return null;
}
