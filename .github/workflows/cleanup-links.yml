name: Cleanup Audit

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: "0 3 * * 1" # Mo 03:00 UTC

jobs:
  links-and-routes:
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: "postgresql://ci:ci@127.0.0.1:5432/ci?schema=public"
      DIRECT_URL: "postgresql://ci:ci@127.0.0.1:5432/ci?schema=public"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install deps
        run: npm ci

      # ---------- 1) Markdown dead links ----------
      - name: Install markdown-link-check
        run: npm i -D markdown-link-check

      - name: Check markdown links
        run: |
          set -e
          # Build list of tracked markdown files (exclude node_modules)
          git ls-files '*.md' | grep -v '^node_modules/' | xargs -r npx mlc --config .github/markdown-link-check.json || exit 1

      # ---------- 2) Guard: obvious dead internal links ----------
      - name: Guard dead internal routes
        run: |
          set -e
          echo "🔎 searching for common dead links..."
          # Beispiel: /profile ist bei dir offenbar falsch -> sollte /account/profile sein
          if grep -R 'href="/profile"' -n src | head -n 1; then
            echo "❌ Found href=\"/profile\". Use /account/profile instead."
            exit 1
          fi

          # Beispiel: alte /downloads route
          if grep -R 'href="/downloads"' -n src | head -n 1; then
            echo "❌ Found href=\"/downloads\". Use /account/downloads instead."
            exit 1
          fi

          echo "✅ no obvious dead internal routes found."
