name: Cleanup Audit

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: "0 3 * * 1" # Mo 03:00 UTC

jobs:
  links-and-routes:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install deps
        run: npm ci --include=dev

      - name: Install markdown-link-check
        run: npm i -D markdown-link-check

      - name: Check markdown links (tracked files only)
        shell: bash
        run: |
          set -euo pipefail

          # Only check markdown files tracked by git (excludes node_modules automatically)
          MD_FILES="$(git ls-files '*.md' '*.mdx' | grep -v '^node_modules/' || true)"
          if [ -z "$MD_FILES" ]; then
            echo "ℹ️ No markdown files found."
            exit 0
          fi

          echo "🔎 Running markdown-link-check on tracked markdown files:"
          echo "$MD_FILES"

          while IFS= read -r f; do
            [ -n "$f" ] || continue
            echo "— checking: $f"
            npx markdown-link-check -c .github/markdown-link-check.json "$f"
          done <<< "$MD_FILES"

      - name: Guard dead internal routes
        shell: bash
        run: |
          set -euo pipefail
          echo "🔎 searching for common dead links..."

          TARGETS=("src")

          PROFILE_REGEX='href[[:space:]]*=[[:space:]]*(\{[[:space:]]*)?["'\'']/profile["'\'']([[:space:]]*\})?'
          DOWNLOADS_REGEX='href[[:space:]]*=[[:space:]]*(\{[[:space:]]*)?["'\'']/downloads["'\'']([[:space:]]*\})?'

          if grep -RIn \
            --include='*.ts' --include='*.tsx' --include='*.js' --include='*.jsx' \
            -E "$PROFILE_REGEX" \
            "${TARGETS[@]}" | head -n 1; then
            echo '❌ Found /profile link. Use /account/profile instead.'
            exit 1
          fi

          if grep -RIn \
            --include='*.ts' --include='*.tsx' --include='*.js' --include='*.jsx' \
            -E "$DOWNLOADS_REGEX" \
            "${TARGETS[@]}" | head -n 1; then
            echo '❌ Found /downloads link. Use /account/downloads instead.'
            exit 1
          fi

          echo "✅ no obvious dead internal routes found."
